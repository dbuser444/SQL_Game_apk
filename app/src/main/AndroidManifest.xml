<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sql_key"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- 1. Главная Активность (Gateway) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Активность для Входа/Регистрации -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- 3. Активность для Уроков (Основной экран) -->
        <activity
            android:name=".LessonsActivity"
            android:exported="false" />

        <activity android:name=".LessonPlayActivity" />

        <!-- Регистрация BroadcastReceiver -->
        <receiver
            android:name=".ReminderBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />


    </application>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
</manifest>
